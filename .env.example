# Variables de entorno para demo rapida (Codespaces/local)
# Copia este fichero a .env y ajusta valores segun tu entorno.

# OpenWeather (opcional en demo; sin clave se muestran mensajes de fallback)
API_WEATHER_KEY=
OWM_API_KEY=
OPENWEATHER_API_KEY=

# Demo ligera: evita dependencia de Hive en presentacion
SIMLOG_ENABLE_HIVE=0

# Timeouts mas tolerantes para primeras ejecuciones en cloud
SIMLOG_INGESTA_TIMEOUT_SEC=600
SIMLOG_SPARK_TIMEOUT_SEC=2400

# Cassandra por defecto en local/compose
CASSANDRA_HOST=127.0.0.1

# Correo SMTP (cuadro de mando: avisos flota).
# Opción A — Gmail: SIMLOG_SMTP_HOST=smtp.gmail.com y contraseña de aplicación (no la clave habitual).
# Opción B — Microsoft 365 / cuentas @educa.* típicas: smtp.office365.com:587 + TLS.
SIMLOG_SMTP_HOST=smtp.office365.com
SIMLOG_SMTP_PORT=587
SIMLOG_SMTP_USER=tu_cuenta@educa.jcyl.es
SIMLOG_SMTP_FROM=tu_cuenta@educa.jcyl.es
SIMLOG_SMTP_PASSWORD=
SIMLOG_SMTP_TLS=1
