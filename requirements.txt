# SIMLOG España — dependencias Python
# Stack Apache 2026

# API y HTTP
requests>=2.28.0
schedule>=1.2.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Kafka
kafka-python>=2.0.2

# Cassandra (driver para app Streamlit)
cassandra-driver>=3.25.0

# HiveServer2 (cuadro de mando Streamlit — sin beeline/subprocess)
pyhive>=0.7.0
thrift>=0.16.0
# PyHive con auth NONE usa TSaslClientTransport; instalar evita «No module named thrift_sasl»
thrift_sasl>=0.4.3

# Config / YAML (catálogo de rutas)
PyYAML>=6.0

# Dashboard
streamlit>=1.28.0
streamlit-folium>=0.15.0
folium>=0.14.0
networkx>=3.0
reportlab>=4.0.0

# Airflow (si se instala en el mismo entorno)
apache-airflow>=2.6.0

# PySpark 3.5 (debe coincidir con Spark en /opt/spark y JAR GraphFrames 3.5)
pyspark>=3.5.0,<4.0
graphframes
